# Referenec

- http://nginx.org/en/docs/beginners_guide.html

# Parser config file (Nginx like HTTP 1.1 compliant)

- plusieurs serveur 
- choisir le port et le host
- pages d'erreur
- limiter client body size


# Exemple config file

http {
	server {
		listen 8080;
		server_name host;
		root /
		location / {
			root /data/www;
		}
		location /images/ {
			/data;
		}
		location /somedir {						# https://www.netsparker.com/blog/web-security/disable-directory-listing-web-servers/
               autoindex on;
        }
	server {
	}
	}
}
